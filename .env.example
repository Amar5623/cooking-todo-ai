# Optional: enables AI-generated natural-language summaries.
# The app works fully without these (falls back to a template-based summary).

# Primary provider (https://console.groq.com)
GROQ_API_KEY=

# Fallback provider (https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=
